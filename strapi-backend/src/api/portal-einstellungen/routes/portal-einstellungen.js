'use strict';

/**
 * portal-einstellungen router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::portal-einstellungen.portal-einstellungen');
