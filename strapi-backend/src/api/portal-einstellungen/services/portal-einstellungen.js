'use strict';

/**
 * portal-einstellungen service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::portal-einstellungen.portal-einstellungen');
